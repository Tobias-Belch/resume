<ul class="inline-list">
  <li class="title">
    <slot name="title"></slot>
  </li>
  <slot></slot>
</ul>

<style>
  .inline-list {
    display: flex;
    justify-content: start;
    flex-wrap: wrap;
    column-gap: calc(1.5 * var(--spacing-xl));
    line-height: 1.3;
  }

  .title {
    margin-inline-start: calc(-1 * var(--spacing-xl));
    list-style: none;
  }
</style>
